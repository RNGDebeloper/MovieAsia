name: movieasia
services:
  - name: web
    docker:
      dockerfile: Dockerfile
    instance_type: free
    ports:
      - port: 3000
        protocol: http
    routes:
      - path: /
        port: 3000
    health_checks:
      - name: web-health
        http:
          path: /api/health
          port: 3000
